export enum AmountSize {
  Sm = 'sm',
  Md = 'md',
  Lg = 'lg',
}

export enum AmountTone {
  Default = 'default',
  Muted = 'muted',
  Accent = 'accent',
}

export type AmountSizeKeys = `${AmountSize}`;
export type AmountToneKeys = `${AmountTone}`;

